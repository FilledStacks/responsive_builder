enum DeviceScreenType {
  watch,
  @Deprecated('Use lowercase version')
  Watch,
  mobile,
  @Deprecated('Use lowercase version')
  Mobile,
  tablet,
  @Deprecated('Use lowercase version')
  Tablet,
  desktop,
  @Deprecated('Use lowercase version')
  Desktop,
}

enum RefinedSize { small, normal, large, extraLarge }
